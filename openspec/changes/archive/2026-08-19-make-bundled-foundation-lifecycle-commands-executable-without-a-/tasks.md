# Tasks

> This is the sole implementation ledger. Check an item only when its verify
> condition passes. Group by coherent behavior, not workflow phase. Add
> `[claims:<claim-id>]` after each stable task ID once evidence claims exist.
>
> Annotations the planner reads, all optional for a single-repository change:
> `[repo:<id>]` (default `root`), `[kind:<kind>]` (default `implementation`),
> `[paths:<glob,glob>]` — declare these to let two tasks in one repository run
> in parallel — `[depends:<T00n,T00n>]`, and `[resources:<token>]` for a
> genuinely shared resource such as a database. A multi-repository change must
> annotate `[repo:]` and `[paths:]` on every task.

- [x] **T001** [claims:bundled-lifecycle-uses-selected-runtime,invalid-bundled-shim-fails-closed,bundled-payload-includes-opencode-installer] Add the upstream OpenCode installer to the required Foundation sync roots and materialize a validated release-bound `claude-foundation` execution shim inside the content-addressed cache without weakening manifest or concurrent-materialization guarantees — bundle sync/runtime and tests — verify: manifest tests require `install-opencode.sh`; runtime tests prove exact delegation, cache reuse, tamper rejection, and no external PATH lookup [repo:root] [paths:packages/opencode/script/sync-foundation.ts,packages/opencode/src/plugin/foundation-runtime.ts,packages/opencode/vendor/claude-foundation/**,packages/opencode/test/plugin/foundation-bundle.test.ts,packages/opencode/test/plugin/foundation-runtime.test.ts]
- [x] **T002** [claims:opencode-host-integration-is-installed,foundation-managed-opencode-commands-use-bundled-runtime,user-owned-opencode-artifacts-are-preserved,bundled-lifecycle-uses-selected-runtime,path-and-inactive-modes-preserve-environment,invalid-bundled-shim-fails-closed] Invoke bundled init/upgrade through `init --host opencode`, classify adapter ownership from the validated Foundation manifest, and wire the shim through Location-scoped `shell.env` for active built-ins or managed adapter commands while preserving PATH mode, opt-out, and genuine user command precedence — initializer/plugin/runtime and focused tests — verify: temporary-project tests cover fresh/repeated host install, canonical commands, guard plugin, manifest ownership, user collisions, bundled/PATH/disabled/mixed activation, shell/background/PTY environment, and materialization failure [depends:T001] [repo:root] [paths:packages/opencode/src/cli/cmd/foundation.ts,packages/opencode/src/plugin/foundation.ts,packages/opencode/src/plugin/foundation-runtime.ts,packages/opencode/test/cli/foundation.test.ts,packages/opencode/test/config/config.test.ts,packages/opencode/test/plugin/foundation.test.ts,packages/opencode/test/plugin/foundation-runtime.test.ts]
- [x] **T003** [claims:compiled-binary-executes-lifecycle-without-path-foundation,bundled-lifecycle-uses-selected-runtime,opencode-host-integration-is-installed,foundation-managed-opencode-commands-use-bundled-runtime,user-owned-opencode-artifacts-are-preserved,bundled-payload-includes-opencode-installer] Extend the Foundation evidence harness and operator documentation so a compiled binary with external Foundation removed from PATH installs the OpenCode host adapter, reloads its native command/plugin surfaces, and executes an actual read-only lifecycle command through the injected environment — evidence script and docs — verify: structured evidence reports all critical cases, compiled deployment smoke passes, repeated install preserves a seeded user collision, and `bun typecheck` passes from `packages/opencode` [depends:T001,T002] [repo:root] [paths:script/evidence-foundation-bootstrap-test.ts,docs/CONFIGURATION.md]
