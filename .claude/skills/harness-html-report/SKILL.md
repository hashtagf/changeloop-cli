---
name: harness-html-report
description: Publish a self-contained HTML report of Change Loop state (gates, evidence, receipts, tasks) as an Artifact. Use when the user asks for a harness, change, or evidence HTML report.
---

# Harness HTML report

A read-only projection of `.foundation/` and `openspec/`; never mutate
harness state or create a parallel status store.

Follow `references/report.md` for data sources, metrics, section structure,
report language, and render/publish rules.
